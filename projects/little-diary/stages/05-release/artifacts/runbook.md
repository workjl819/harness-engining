# 运行手册

## 启动方式
- 直接在浏览器打开 `apps/little-diary/index.html`
- 或使用任意静态服务器托管 `apps/little-diary/`
- 示例：`python3 -m http.server` 后访问本地静态目录

## 常见问题
- 如果页面没有数据，先确认当前浏览器是否已清除本地存储。
- 如果样式丢失，检查 `styles.css` 是否与 `index.html` 同目录。
- 如果无法保存，检查浏览器是否禁用了本地存储能力。
- 如果在手机上打开异常，优先检查页面是否完整加载了静态资源。
