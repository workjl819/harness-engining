# 架构讨论 R001

## 背景
需求阶段已经明确：小小日记是手机端 H5 demo，只要本地保存，不做登录注册。

## 讨论结果
- 采用纯前端方案，不需要后端。
- 本地存储优先使用 `localStorage`，满足 demo 阶段即可。
- 页面只保留列表页和编辑页两个核心入口。

## 结论
这个 demo 的最佳实现是“静态 H5 + 浏览器本地存储 + 轻量页面结构”，不引入远程 API。
