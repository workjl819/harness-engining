# 部署计划

## 结论
这是一个纯前端静态项目，可直接通过静态托管发布。
当前版本不需要后端部署，也不需要数据库、任务队列或服务端进程。

## 发布方式
- 本地直接打开 `apps/little-diary/index.html`
- 或放到任意静态托管服务

## 前置条件
- 浏览器支持 `localStorage`
- 保证 `index.html`、`styles.css`、`app.js` 在同一路径下可访问
- 发布目标环境不拦截静态资源访问
